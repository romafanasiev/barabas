# M1-02 — ER-диаграмма домена

Status: resolved

## Задача

Нарисовать ER-модель всего домена в Mermaid и положить в `docs/`. Диаграмма — не картинка для
красоты, а **источник, из которого пишутся миграции `03`–`05`**: если связи нет на диаграмме,
её нет и в схеме.

Сверить с [CONTEXT.md](../../../CONTEXT.md): каждая сущность на диаграмме обязана быть в
словаре, каждое понятие словаря — найтись на диаграмме или получить объяснение, почему его там
нет (например, `Reconciliation` — это запрос, а не таблица).

## Definition of Done

- `docs/` содержит диаграмму в Mermaid, она рендерится
- Имена сущностей и полей — из `CONTEXT.md`, без синонимов из списка `_Avoid_`
- Для каждой связи проставлена cardinality (детально — в `09`)
- Отмечено, где проходят границы агрегатов (детально — в `13`)
- Расхождения со словарём либо устранены, либо выписаны как правки в `CONTEXT.md`

## На что смотрю на ревью

- Нет сущностей «на будущее», которых не требует ни один сценарий из `MISSION.md`
- Нумерованная и ненумерованная секция — видно, что это два разных способа учёта
  (`Seat` против счётчика), а не одна таблица с `nullable`-полями на все случаи
- `Hold` и `Order` — разные сущности с разным временем жизни, это видно на диаграмме
- `Ticket` не может существовать до оплаты — видно по связям

## Решено на триаже (2026-09-15)

Ниже — то, что уже верно про твой репозиторий и твой словарь, чтобы диаграмма не начиналась
с чистого листа.

**Перечитай [урок 0005](../../../lessons/0005-venue-that-moved-halfway.html) перед стартом.**
Он ровно про этот issue: три аномалии, 1NF→3NF на этом домене и граница осознанной
денормализации. `M1-12` (счётчик `available` вместо `COUNT(*)`) — прямое продолжение, и на
диаграмме ты уже столкнёшься с этим выбором.

**Механика Mermaid, которой хватит на всё.** Проверено по докам `erDiagram`:

- Cardinality — два символа с каждой стороны, внешний задаёт максимум, внутренний минимум:
  `||` ровно один, `|o` ноль или один, `}o` / `o{` ноль или больше, `}|` / `|{` один или больше.
- **Identifying против non-identifying**: `--` рисует сплошную линию, `..` пунктирную. Смысл
  не косметический: identifying означает, что дочерняя сущность **не может существовать без
  родителя**. Это готовый механизм, чтобы показать композицию внутри агрегата, — подумай,
  прежде чем изобретать свой.
- У атрибутов бывают ключи `PK`, `FK`, `UK` (через запятую: `PK, FK`) и комментарий в двойных
  кавычках в конце строки. **Ловушка:** внутри комментария двойных кавычек быть не может,
  парсер падает.

**Расхождение со словарём, которое ты найдёшь.** В `CONTEXT.md` сказано, что Hold «принадлежит
ровно одному покупателю», в value objects есть `Email`, а в разделе «Где DDD не применяется» —
«профили пользователей». Но **самой сущности покупателя в Ubiquitous language нет** — ни
`Customer`, ни `User`. Диаграмму без неё не нарисовать. Это ровно тот случай, который DoD
называет «расхождения выписать как правки в `CONTEXT.md`»: имя выбираешь ты, и оно станет
именем таблицы, класса и эндпоинта на весь проект.

**Пять агрегатов уже названы** в таблице «Агрегаты и их инварианты»: `Seat`, `TicketAllocation`,
`Hold`, `Order`, `Ticket`. Детальное обоснование границ — задача `M1-13`, но на диаграмме они
должны быть видны уже сейчас, иначе `M1-13` будет нечего обосновывать.

**Главный вопрос этого issue — не как рисовать, а два механизма учёта.** Словарь говорит:
для нумерованных секций единица учёта — `Seat`, для ненумерованных — счётчик
(`TicketAllocation` с `held`, `sold`, `capacity`). Отсюда три связанных решения, и все три
твои:

1. Как `TicketType` добирается до того, что он продаёт. Словарь связывает `TicketType` с
   `Event` и с ценой, но **не говорит, привязан ли он к `Section`** — а без этого непонятно,
   какие именно места отдаёт `Standard`, а какие `VIP`.
2. Чем `Hold` ссылается на удерживаемое, когда в одном случае это конкретная строка `Seat`,
   а в другом — количество в счётчике.
3. Что происходит с этими же связями у `Order` и `Ticket` — они наследуют ту же развилку.

**Ловушка, из-за которой issue возвращают:** обе ветки сводят в одну таблицу с двумя
`nullable` FK (`seat_id` или `allocation_id`, одно из двух заполнено). Это выглядит экономно и
убивает возможность выразить инвариант constraint'ом — база не умеет требовать «ровно одно из
двух» без дополнительного `CHECK`, а `M1-06` с этого начнётся. Решение может быть и таким, но
тогда назови его цену на ревью сам.

**Reconciliation на диаграмме быть не должно** — это запрос, а не таблица. Скажи это явным
комментарием, иначе на ревью я решу, что ты про него забыл.

## План работ

Отмечай пункты по ходу прямо здесь.

- [x] **1. Ветка.** `02-er-diagram` от актуальной базовой ветки M1 (`01-adr-data-layer` ещё не
      в `main` — либо смержи его, либо ветвись от него и скажи об этом).
      *Готово, когда:* `git status` чистый, ветка своя.

- [x] **2. Инвентаризация словаря.** Выпиши в черновик все понятия из `Ubiquitous language` и
      против каждого пометь: таблица / поле / вычисляется запросом / часть агрегата, но не
      таблица. Это список, по которому потом сверяется DoD.
      *Готово, когда:* у каждого понятия из `CONTEXT.md` стоит пометка, включая
      `Reconciliation` и `CheckIn`.

- [x] **3. Сущности без связей.** Нарисуй только прямоугольники с атрибутами и ключами
      (`PK`, `FK`, `UK`), без единой линии. Типы уже пиши целевые: `timestamptz`, `numeric`,
      а не «дата» и «цена».
      *Готово, когда:* диаграмма рендерится, и в ней нет ни одного имени из списков `_Avoid_`.

- [x] **4. Покупатель.** Закрой расхождение из триажа: выбери имя сущности, добавь определение
      в `CONTEXT.md` тем же форматом, что у остальных (включая `_Avoid_`), и поставь её на
      диаграмму.
      *Готово, когда:* в `CONTEXT.md` есть новая статья словаря, и в diff видно только её.

- [x] **5. Связи с cardinality.** Проставь каждой связи оба конца. Для каждой спроси себя
      вслух: «может ли левое существовать без правого» — это и есть выбор между `||` и `|o`.
      **Точка решения:** какие связи identifying (`--`), какие нет (`..`). Ответ должен
      совпадать с границами агрегатов, а не быть случайным.
      *Готово, когда:* в диаграмме нет ни одной линии без cardinality с обеих сторон.

- [x] **6. Два механизма учёта.** Разведи нумерованную и ненумерованную секцию так, чтобы это
      было видно глазом. Сюда же — решение по `TicketType` ↔ `Section` из триажа.
      **Точка решения:** твоя, и она определит `M1-04`. Назовёшь её на ревью словами.
      *Готово, когда:* на диаграмме видно, что `Seat` и счётчик — разные пути, и ни один
      `Event` не может случайно оказаться учтён обоими сразу.

- [x] **7. `Hold` → предмет удержания.** Самый тяжёлый пункт. Реши, чем `HoldItem` ссылается
      на место или на квоту, и проверь своё решение вопросом: **какой constraint запретит
      два активных Hold на одно место?** Если ответа нет — решение не готово, а `M1-10`
      (partial unique index) будет не на чем строить.
      *Готово, когда:* можешь показать пальцем на диаграмме, где именно живёт уникальность
      «одно место — один активный Hold».

- [x] **8. `Order`, `Payment`, `Ticket`.** Дострой цепочку до оплаты и выпуска билета.
      Проверка из раздела «На что смотрю»: по связям должно быть видно, что `Ticket` не может
      появиться раньше оплаты.
      *Готово, когда:* можешь провести пальцем путь `Hold → Order → Payment → Ticket → CheckIn`
      и на каждом шаге назвать, что мешает перепрыгнуть через звено.

- [x] **9. Границы агрегатов.** Отметь пять агрегатов из `CONTEXT.md`. Способ выбираешь сам
      (identifying-линии, комментарии, легенда), но он должен быть один и тот же по всей
      диаграмме.
      *Готово, когда:* каждый из пяти агрегатов находится на диаграмме за пять секунд, и
      видно, какие сущности внутри него, а какие — ссылки по id снаружи.

- [x] **10. Сверка с инвариантами — аналог мутации.** Пройди таблицу «Агрегаты и их
      инварианты» из `CONTEXT.md` сверху вниз и для каждого инварианта скажи, **чем на
      диаграмме он держится**: ключом, связью, отдельной таблицей — или «ничем, это код
      агрегата». Инвариант без ответа — дыра в модели, а не мелочь.
      *Готово, когда:* у всех инвариантов из таблицы есть ответ, и список этих ответов лежит
      текстом рядом с диаграммой.

- [x] **11. Рендер и гейты.** Диаграмма должна отрендериться не только в голове.
      *Готово, когда:* рендер прошёл без ошибок парсера, `pnpm format:check` и `pnpm lint`
      зелёные, `git status` не содержит лишнего.

## Что спрошу на ревью словами

- Покажи на диаграмме место, где физически невозможно продать одно место дважды. Если такого
  места нет, скажи, в каком issue оно появится и почему не здесь.
- Ненумерованная секция: где на диаграмме живёт `capacity` и почему именно там, а не рядом с
  `Section`.
- Одну связь, которую ты добавил «потому что логично», а не потому что её требует сценарий из
  `MISSION.md`. Если такой нет — скажи, что нет.

## Comments

**Ревью 2026-09-16 (агент), состояние плана.**

**Пункт 7 — закрыт.** Уникальность «одно место — один активный Hold» живёт в partial unique
index `hold_item (event_seat_id) WHERE status = 'active'`. Путь к решению: `HoldStatus` как
отдельная сущность → `EventSeat` как строка состояния → `HoldItem` как связь `Hold` ↔
`EventSeat`. Выбрана ветка «гарантия в схеме», а не «гарантия в дисциплине `UPDATE`»;
поддержка в Drizzle подтверждена спайком из [ADR-0004](../../../docs/adr/0004-data-layer-drizzle.md).
Решения и их цена выписаны в
[docs/db/states-and-invariants.md](../../../docs/db/states-and-invariants.md).

**Пункт 2 — снял галочку.** Артефакта инвентаризации в репозитории нет: `Reconciliation`
на диаграмме не помечен комментарием, `TicketAllocation` из таблицы агрегатов отсутствует,
value objects не размечены. Признак готовности — «у каждого понятия из `CONTEXT.md` стоит
пометка» — проверить не по чему.

**Пункт 3 — снял галочку.** Осталось два хвоста против «типы уже пиши целевые»:
`enumerated type status` вместо имени enum-типа (и это, скорее всего, ещё и ломает парсер —
три слова подряд в атрибуте) и `Inventory.ticket_type` без суффикса `_id`. Рендер не показан.

**Пункт 10 — продвинут, но не закрыт.** Таблица инвариантов заполнена, четыре строки без
ответа: `held + sold <= capacity` и `available >= 0` (ждут пункта 6), `total = Σ(item × qty)`
(ждёт пункта 8), плюс две дыры, закрываемые дёшево прямо сейчас — у `payment` нет ключа
идемпотентности, у `check_in.ticket_id` нет `UNIQUE`.

**Пункт 3 — закрыт 2026-09-16.** Рендер подтверждён плагином IDE. Целевые типы везде, включая
именованные enum: `hold_status`, `hold_item_status`, `order_status`, `user_role`. Имён из
списков `_Avoid_` нет — `Customer` ушёл последним. 16 сущностей, ни одной связи, как и требует
пункт. Диаграмма лежит markdown-файлом с блоком ` ```mermaid `, а не `.mmd`: так она
рендерится и в IDE, и на GitHub.

**Пункт 4 — закрыт 2026-09-16.** Выбран один `User` с ролью (`buyer` / `organizer` / `scanner`
из `M3-03`); отдельной сущности покупателя нет — покупатель это роль. Статья добавлена в
`CONTEXT.md`, diff — только она, `Customer` ушёл в `_Avoid_`. На диаграмме `Customer` удалён,
`hold.customer_id` → `user_id`.

**Отход от режима.** [docs/db/states-and-invariants.md](../../../docs/db/states-and-invariants.md)
и статья `User` в `CONTEXT.md` написаны агентом по прямому решению Романа; решения в них —
Романа, текст — агента. Ср. [learning-records/0002](../../../learning-records/0002-review-driven-workflow.md).

**Найдено попутно.** `pnpm format:check` не покрывает корневые файлы: `pnpm-workspace.yaml`
содержит только `apps/*`, а скрипт — это `pnpm -r format:check`. `docs/`, `.scratch/` и
корневые markdown мимо гейта. Пункт 11 считает его зелёным, хотя проверки нет.

**Старт пунктов 5 и 6, 2026-09-17 (агент).** Выдан
[урок 0007](../../../lessons/0007-hold-that-does-not-know-what-it-holds.html) — форма развилки
«место или квота»: почему инвариант над множеством строк невыразим `CHECK`'ом, четыре формы
(nullable-пара, супертип, две таблицы, отказ от развилки) и цена каждой. Решение по пункту 6
жду в `## Comments` **до** правок диаграммы, двумя списками: что держит схема, что переезжает
в код агрегата.

Состояние диаграммы на этот момент, всё видно без единой связи:

- Счётчика в схеме нет. `Inventory { ticket_type_id, total_seats }` — одно число вместо
  `held` / `sold` / `capacity`; инвариант `held + sold <= capacity` держать нечем. Плюс
  расхождение имени: на диаграмме `Inventory`, в таблице агрегатов `TicketAllocation`.
- От `Inventory` нет пути до `Event`. `TicketType` не ссылается ни на `Event`, ни на `Section`,
  единственная связь — через `TicketPrice (ticket_type_id, event_id)`, то есть «многие ко
  многим». Вопрос «сколько VIP осталось на концерт X» схемой не отвечается. Это развилка 1
  из триажа, без неё не решается развилка 2.
- `HoldItem` умеет держать только место: `event_seat_id` и всё, ни ссылки на квоту, ни
  `quantity`. Та же дыра ждёт `OrderItem` (сейчас не ссылается вообще ни на что продаваемое)
  и `Ticket` (`seat_id`, который для ненумерованного билета будет пустым).
- `Section.type` — `varchar(255)`, хотя именно он решает, по какой из двух веток идёт учёт.
  Именованные enum в других местах уже есть. Рядом: `User.role` объявлен типом `role`, а в
  комментарии к пункту 3 зафиксирован `user_role`.

Закрыто с прошлого ревью: у `check_in.ticket_id` появился `UNIQUE`, у `Inventory.ticket_type`
— суффикс `_id`. У `payment` ключа идемпотентности по-прежнему нет.

**Пункт 2 сдан на проверку, 2026-09-17 (агент, делегировано).** Артефакт —
[docs/db/language-to-schema.md](../../../docs/db/language-to-schema.md): пометка у каждого
понятия `Ubiquitous language`, у пяти агрегатов, у шести value objects и у доменных событий;
сверка идёт в обе стороны.

Что нашла сверка: четыре сущности на диаграмме без статьи в словаре (`EventSeat`,
`TicketAllocation`, обе таблицы позиций, `OrderItem`) плюс неназванные значения
`section_type`; статья `Inventory` описывает механизм, у которого нет одноимённой таблицы;
статья `Hold` описывает набор `HoldItem`, которого после решения 6 не существует; `Ticket`
дублирует пару `(event_id, seat_id)` вместо ссылки на `event_seat` и висит на `payment_id`
вместо `order_id` — последнее оставлено пункту 8.

**Чего агент не делал:** не писал новых статей в `CONTEXT.md` и не правил существующие —
статья включает `_Avoid_`, то есть это выбор имён, а он за Романом. Галочку в плане не
ставил: признак готовности — «у каждого понятия стоит пометка», проверяется чтением.

**Пункт 5 принят, 2026-09-17 (агент).** Пятнадцать связей, маркеры валидны, правило
identifying/non-identifying применено последовательно: сплошная там, где родитель входит в
ключ ребёнка (каталог, `TicketType` внутри события, обе связи `EventSeat` и
`TicketAllocation`, обе ветки позиций холда), пунктир — ссылки по id между самостоятельными
сущностями (`Venue → Event`, `TicketType` к предметам продажи, `User → Hold`, позиции холда
к тому, что держат).

Два места, где выбор был неочевиден и сошёлся верно: `EventSeat ..o{ HoldSeatItem` —
позиций на место много, активна не больше одной, и это держит индекс, а не линия;
`Section ||--o{ TicketAllocation` при `UK (event_id, section_id)` — квот у секции много по
разным событиям, но не больше одной на событие.

Решения, принятые по ходу и записанные в
[states-and-invariants.md](../../../docs/db/states-and-invariants.md): 6 (две независимые
таблицы позиций, слияние веток после Hold), 7 (счётчик `TicketAllocation`), 8 (цена в
`ticket_type`, `TicketPrice` удалён), 9 (учёт секции одним способом — дискриминатор плюс
составной FK), 10 (агрегаты помечаются комментарием у атрибута), 11 (место в событии всегда
продаётся).

Не сделано и остаётся за Романом: рендер пятнадцати линий (парсер их целиком ещё не
собирал), подписи связей — часть читается справа налево после разворота порядка, пометки
агрегатов (пункт 9). Ветка `Order → Payment → Ticket → CheckIn` намеренно оставлена без
связей до пункта 8.

**Пункт 8, решения приняты 2026-09-17 (агент записал, решения Романа).** Четырнадцать решений,
12–25, в [states-and-invariants.md](../../../docs/db/states-and-invariants.md). Главное:

- `order_item` и `ticket` ссылаются на покупаемое парой nullable FK с `CHECK` — форма из
  урока 0007, выбрана сознательно и только ниже `Hold` (12, 16);
- один заказ на один холд, `hold_id NOT NULL UNIQUE`; покупатель — джойном через `hold.user_id`
  (13). Заказа в обход удержания нет, иначе продажа шла бы мимо partial unique index;
- успешный платёж неизменяем, возврат переезжает в `M3` (14);
- билет ссылается только на **успешный** платёж: составной FK на `payment (id, status)` —
  «Ticket не раньше оплаты» стало гарантией схемы (15);
- второй рубеж против двойной продажи: `UNIQUE ticket (event_seat_id) WHERE NOT NULL` (17);
- `payment` получает `pending → succeeded | failed`, оба исхода терминальны; строка создаётся
  **до** обращения к провайдеру, иначе идемпотентность не работает; два уникальных ключа —
  по `idempotency_key` и по `(order_id) WHERE succeeded` (18–20);
- валюта — справочник `currency` плюс составной FK `(order_id, currency)` из `payment` и
  `order_item`; `ticket` сознательно вне правила (21, 22);
- `fulfilled` из `order_status` убран — выпуск билетов виден по наличию `ticket` (23);
- `total` не хранится вообще: инвариант снят удалением колонки, а не механизмом
  синхронизации (24);
- оплаченный заказ отменить нельзя, `paid` терминален до появления возврата в `M3` (25).

Правки словаря: статья `Order` (нет `fulfilled`), `Money` (валюта из справочника).

В таблице инвариантов осталось два ❌: механизм для `capacity <= section.capacity` (решение 5,
не выбран) и валюта билета против валюты платежа (решение 22, сознательно на тесте).

Не сделано: связи ветки заказа на диаграмме, колонки `payment` и `ticket` под решения 15–22,
удаление `order.total_price`, `seat_id`, `event_id` из `ticket`.

**Issue закрыт 2026-09-17.** Одиннадцать пунктов плана отмечены. Итог: 16 сущностей,
30 связей, 25 записанных решений в
[states-and-invariants.md](../../../docs/db/states-and-invariants.md), карта понятий в
[language-to-schema.md](../../../docs/db/language-to-schema.md), границы агрегатов и таблица
инвариантов — рядом с диаграммой в [er-model.md](../../../docs/db/er-model.md).

Правки словаря по ходу: добавлена статья `EventSeat`, агрегат `Seat` переименован в
`EventSeat`, `Inventory` стал зонтичным понятием, `Order` потерял `fulfilled`, `Money` знает
про справочник `Currency`, `Hold` описан через две таблицы позиций.

**Хвосты, уходящие за пределы issue:**

- `pnpm format:check` и `pnpm lint` зелёные, но `pnpm-workspace.yaml` содержит только
  `apps/*`: корневые markdown, `docs/` и `.scratch/` мимо гейта. Для issue, продукт которого —
  три markdown-файла, это значит, что гейт пункта 11 не проверил ничего. **Решено 2026-09-17:
  покрытие не расширяем.** Prettier недоволен 19 файлами, и приведение их к его стилю — разовый
  diff во всех документах проекта ради косметики; документы читают люди, а не компилятор.
  Гейты остаются про код в `apps/*`. Знать об этом нужно ровно одно: «зелёный `format:check`»
  ничего не говорит о markdown.
- Рендер после двух последних правок агента (пометки агрегатов, `section_capacity`)
  подтверждает Роман.
- Единственный `❌` в таблице инвариантов — валюта билета против валюты платежа, оставлена
  тесту сознательно (решение 22).
